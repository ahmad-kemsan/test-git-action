name: 'Update Version'

description: 'Update the package version'

inputs:
  release-type:
    description: 'Type of release (patch, minor, major)'
    required: false
    default: 'patch'
  token:
    description: 'token having write permissions'
    required: true
  url:
    description: 'repo url'
    required: true

runs:
  using: 'node16'
  main: 'index.js'